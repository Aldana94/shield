'use client';

import PageWrapper from '@/components/PageWrapper';
import NebulaSlotCreator from '@/components/NebulaSlotCreator';

export default function NebulaPage() {
  return (
    <PageWrapper>
      <NebulaSlotCreator />
    </PageWrapper>
  );
}